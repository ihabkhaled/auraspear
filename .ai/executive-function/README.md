# Executive Function

The boot layer establishes control before planning. The canonical rules define behavior. The state machine exposes transitions for tooling. Trigger-specific skills prevent the framework itself becoming a context tax.
