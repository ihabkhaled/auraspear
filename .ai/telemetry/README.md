# Operational Signals

These schemas capture counts and outcomes, not chain-of-thought. Tooling may compare signals with `thresholds.json` to trigger attention reset, strategy reset, or blocker escalation.
