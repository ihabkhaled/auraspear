# Knowledge Maintenance

Keep one canonical source and thin adapters. Change the smallest authoritative source, update affected indexes/manifests, validate references and JSON, and avoid manual duplicated copies. Repository source beats generated context and stale memory. Framework changes must preserve useful repository-specific instructions and pass `npm run ai:check`.
