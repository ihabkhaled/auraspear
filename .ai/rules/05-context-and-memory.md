# Context and Memory

Use L0 boot, L1 task routing, L2 domain context, L3 exact sources, and L4 deep architecture only when required. On context pressure, compress Goal, Completed, Remaining, Decisions, Evidence, Blocker, Deferred, Next. Durable memory accepts only stable, repeatedly useful, architectural, costly-to-rediscover facts; source changes invalidate stale summaries.
