# Context Router

Load `.ai/bootstrap/boot.toon`, then select one domain in `context-index.json`. Read exact changed files before trusting summaries. Deep architecture is opt-in. Current-task and state files are templates, not automatically authoritative session state.
