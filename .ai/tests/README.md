# Framework Self-tests

`npm run ai:check` validates JSON, schemas, manifests, adapter references, state-machine targets, instruction dependencies, boot size, and scenario coverage. `npm test -- test/ai-framework.test.ts` supplies repository regression coverage.
