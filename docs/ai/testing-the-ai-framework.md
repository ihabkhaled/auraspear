# Testing the AI Framework

Run `npm run ai:check` for structural integrity and `npm test -- test/ai-framework.test.ts` for regression coverage. The validator reports counts for rules, skills, routers, knowledge, broken references, duplicates, JSON/schema health, context routes, machine outputs, boot size, cycles, and required behavioral scenarios.
