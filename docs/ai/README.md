# AI Agent Architecture

The operational framework lives in `.ai`. Its complete specification is `.ai/MASTER-PROMPT.md`; the tiny runtime boot is `.ai/bootstrap/boot.toon`; canonical behavior is `.ai/rules/00-master-rules.md`. Repository implementation requirements remain in `CLAUDE.md`. Run `npm run ai:check` after changing any layer.
